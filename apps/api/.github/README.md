# Inactive — do not use

These workflow files are Laravel framework skeleton leftovers.

GitHub Actions only runs workflows from the **monorepo root**:

`/.github/workflows/`

Active pipelines:

- `ci.yml` — API tests + web build
- `retail-smoke.yml` — retail path gate
- `deploy.yml` — Next.js VPS deploy

Do not add new jobs here.
