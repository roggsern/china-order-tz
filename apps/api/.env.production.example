# DEPRECATED — use the monorepo root production template instead:
#
#   cp .env.production.example .env
#
# Authoritative template: /.env.production.example (project root)
# See docs/OPERATIONS.md and docs/PRODUCTION_OWNERSHIP.md
