; Product / catalog media upload limits (production contract).
; Copied into $PHP_INI_DIR/conf.d/ for api, queue, and scheduler images.
; Keep aligned with Laravel ProductMediaUploadContract (10 MB) and Nginx (>=10 MB).

upload_max_filesize = 10M
post_max_size = 12M
memory_limit = 256M
